# ── System dependencies (Linux / WSL) ─────────────────────────────────────
# Install once with apt before running pip install:
#
#   sudo apt update && sudo apt install -y \
#     build-essential cmake git \
#     libopenblas-dev python3-dev python3-pip
#
# --------------------------------------------------------------------------

# ── Notebook ───────────────────────────────────────────────────────────────
jupyter>=1.0.0
notebook>=7.0.0

# ── Core inference ─────────────────────────────────────────────────────────
# Install with SIMD flags (AVX2 + OpenBLAS) for 10-20 tok/s instead of 0.2:
#
#   CMAKE_ARGS="-DGGML_BLAS=ON -DGGML_BLAS_VENDOR=OpenBLAS -DGGML_AVX2=ON -DGGML_AVX=ON" \
#     pip install llama-cpp-python --force-reinstall --no-cache-dir
#
# Or use the pre-built AVX2 wheel (no compiler needed):
#

#llama-cpp-python --extra-index-url "https://abetlen.github.io/llama-cpp-python/whl/cpu"
llama-cpp-python==0.3.20

# ── System monitoring ──────────────────────────────────────────────────────
psutil>=5.9.0
py-cpuinfo>=9.0.0

# ── Model download ─────────────────────────────────────────────────────────
huggingface_hub>=0.27.0

# ── GGUF conversion (cpu_optimizer / convert_hf_to_gguf.py) ───────────────
torch>=2.1.0
sentencepiece>=0.2.0
transformers>=4.40.0
tokenizers>=0.21.0
python-dotenv==1.2.2
